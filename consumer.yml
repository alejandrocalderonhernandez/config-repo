product:
  iva: 0.16
  desc: impuesto de valor agregado
  defaultMethod: "defaultCircuitBreaker"
  
kafka:
  topic:
    name: ${kafka.topic.name}
  server:
    host: ${kafka.server.host}
    port: ${kafka.server.port}
  batch:
    size: ${config.kafka.batch.size}
  retires: ${kafka.retires}
  linger: ${kafka.linger}
  
